{
    "name": "talkie",
    "private": true,
    "scripts": {
        "start": "concurrently \"cd frontend && npx expo start\" \"cd backend && node server.js\"",
        "dev:frontend": "cd frontend && npx expo start",
        "dev:backend": "cd backend && node server.js"
    },
    "dependencies": {
        "concurrently": "^8.2.0"
    },
    "devDependencies": {
        "@types/react-native": "^0.73.0"
    }
}
